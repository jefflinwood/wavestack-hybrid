name: adaptation_pretrain
model:
  use_analytical_decomp: true
  hidden_dim: 512
  decomposition:
    causal: true
    poly_order: 4
training:
  learning_rate: 0.00025
  batch_size: 32
  max_steps: 6000
dataset_name: roneneldan/TinyStories
