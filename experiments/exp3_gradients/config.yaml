name: gradient_probe
model:
  use_analytical_decomp: true
  hidden_dim: 256
  decomposition:
    causal: true
    poly_order: 4
training:
  learning_rate: 0.0005
  batch_size: 16
  max_steps: 2000
dataset_name: roneneldan/TinyStories
